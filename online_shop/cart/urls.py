from django.urls import path, include
from rest_framework.routers import DefaultRouter
from . import views

route = DefaultRouter()

# route.register(r'', views.)

urlpatterns = [
    # path('', include(route.urls)),
    # path('', views..as_view()),
    # path('', views..as_view()), 
]